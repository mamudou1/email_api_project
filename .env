EMAIL_HOST=smtp.gmail.com      # SMTP server address for sending emails (Gmail)
EMAIL_PORT=587                 # SMTP server port for TLS (Gmail)
EMAIL_USER=your_email@gmail.com # Email address used for authentication
EMAIL_PASS=your_app_password   # App-specific password for the email account
IMAP_SERVER=imap.gmail.com     # IMAP server address for receiving emails (Gmail)
IMAP_PORT=993                  # IMAP server port for SSL
